@model OperationResult
@using EM.Calc.Web.Models
@{
    Layout = null;
}
@Html.ValidationSummary(false, "", new { @class = "text-danger" })
@if (!string.IsNullOrWhiteSpace(Model.Error))
{
    <b style="color:red;font-size:300%;">@Model.Error</b>
}
else
{
    @Model.Result
}